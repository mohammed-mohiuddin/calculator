import * as types from '../ActionTypes';

// ======== ADD_INPUT ==========
describe('actions types', () => {
    it('should return an action type ADD_INPUT', () => {
      const ADD_INPUT = 'ADD_INPUT'
      expect(types.ADD_INPUT).toEqual(ADD_INPUT)
    })
  })

// ======== REMOVE_INPUT ==========
describe('actions types', () => {
    it('should return an action type REMOVE_INPUT', () => {
      const REMOVE_INPUT = 'REMOVE_INPUT'
      expect(types.REMOVE_INPUT).toEqual(REMOVE_INPUT)
    })
  })

  // ======== CLEAR_INPUT ==========
describe('actions types', () => {
    it('should return an action type CLEAR_INPUT', () => {
      const CLEAR_INPUT = 'CLEAR_INPUT'
      expect(types.CLEAR_INPUT).toEqual(CLEAR_INPUT)
    })
  })

  // ======== ADD_RESULT ==========
describe('actions types', () => {
    it('should return an action type ADD_RESULT', () => {
      const ADD_RESULT = 'ADD_RESULT'
      expect(types.ADD_RESULT).toEqual(ADD_RESULT)
    })
  })

  // ======== COPY_RESULT ==========
describe('actions types', () => {
    it('should return an action type COPY_RESULT', () => {
      const COPY_RESULT = 'COPY_RESULT'
      expect(types.COPY_RESULT).toEqual(COPY_RESULT)
    })
  })

  // ======== PASTE_RESULT ==========
describe('actions types', () => {
    it('should return an action type PASTE_RESULT', () => {
      const PASTE_RESULT = 'PASTE_RESULT'
      expect(types.PASTE_RESULT).toEqual(PASTE_RESULT)
    })
  })

  // ======== ADD_LABELS ==========
describe('actions types', () => {
    it('should return an action type ADD_LABELS', () => {
      const ADD_LABELS = 'ADD_LABELS'
      expect(types.ADD_LABELS).toEqual(ADD_LABELS)
    })
  })